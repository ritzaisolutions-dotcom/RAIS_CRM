import * as React from "react";
import { cn } from "@/lib/utils";

const Input = React.forwardRef<HTMLInputElement, React.ComponentProps<"input">>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          "flex h-9 w-full rounded-md border border-rais-border bg-white px-3 py-1 text-sm text-rais-charcoal shadow-sm transition-colors placeholder:text-rais-stone focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-rais-orange/40 disabled:cursor-not-allowed disabled:opacity-50",
          className,
        )}
        ref={ref}
        {...props}
      />
    );
  },
);
Input.displayName = "Input";

export { Input };
